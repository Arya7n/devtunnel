# @devtunnel/ui

Shared React UI primitives for the dashboard (and future surfaces).

## Exports

- `Button` — primary / ghost variants
- `./globals.css` — shared token hooks

Consumed via workspace import; source is transpiled by Next.js (`transpilePackages`).

## Scripts

```bash
pnpm --filter @devtunnel/ui typecheck
```
